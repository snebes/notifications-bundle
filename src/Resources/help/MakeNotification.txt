The <info>%command.name%</info> command generates a new notification class.

<info>php %command.full_name% OrderNotification</info>

If the argument is missing, the command will ask for the class name interactively.
